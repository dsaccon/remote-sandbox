# User-specific config; copy config.example to config and edit
config
*.local

# Secrets / credentials — NEVER commit
.env
.env.*
*.pem
*.key

# AWS CLI cache
.aws-cache/

# Local state
state/

# macOS
.DS_Store
